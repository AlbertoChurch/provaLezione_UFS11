name: il mio primo git hubaction
on:
  push:
jobs:
  italian-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4 
      - run: ls
      - run: sudo apt update
      - run: pwd
      - run: ls -la
      - run: java --version
      - run: ls
      - run: pwd
        working-directory: cd template-progetto
      - run: mvn install
        working-directory: cd template-progetto
